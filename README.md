# 数智施工演示环境

这是一个用于产品演示的静态前端 Demo，已整理为适合 GitHub Pages 发布的目录结构。

当前版本：`EM-20260701-V2.2.632-ECONOMY-REPORT-SERIES4-5-CHARTS`

## 在线发布

将本目录中的所有文件上传到 GitHub 仓库根目录，然后开启 GitHub Pages：

1. 进入仓库 `Settings`。
2. 打开 `Pages`。
3. Source 选择 `Deploy from a branch`。
4. Branch 选择 `main`，Folder 选择 `/root`。
5. 保存后等待 GitHub 生成访问地址。

访问地址通常为：

```text
https://你的用户名.github.io/仓库名/
```

## 目录结构

```text
.
├── index.html              # 页面入口
├── app.js                  # 页面逻辑与模拟数据
├── styles.css              # 全局样式
├── organization-data.js    # 组织基础数据
├── src/                    # 图片、图标、组件资源
├── data/                   # 演示数据与数据结构占位
├── docs/                   # 文档资料
├── CHANGELOG.txt           # 历史版本说明
└── .nojekyll               # GitHub Pages 静态资源开关
```

## 本地预览

可以直接使用任意静态服务器预览，例如：

```bash
npx serve .
```

或使用 VS Code 的 Live Server 插件打开 `index.html`。

## 注意事项

- GitHub Pages 只能托管静态文件，不能运行后端服务。
- 当前在线版本适合页面演示、交互预览和同事评审。
- 数据字典、组织、人员等在线维护后的服务端持久化，需要后续接入后端接口或使用本地服务版本。

## 当前版本

`EM-20260701-V2.2.632-ECONOMY-REPORT-SERIES4-5-CHARTS`

本版本重新分配月度检验单列宽，压缩序号、组织、项目及基础指标列，将空间平均补充给一二级预警列；主体内容和一级预警提升至 14px，二级预警标题统一为 12px。

本版本替换项目经济分析报告预览第一页的品牌资产：左上角使用隧道股份正式 Logo，整页使用指定背景图横向铺满，移除原程序绘制的临时 Logo 和装饰背景。

本版本将企业管理 / 经济 / 大屏看板 / 经济诊断中的月度检验单升级为默认全屏 H5，参考企业级月度体检报告按子公司分段展示全部项目，提供汇总统计、公司快速定位、多级预警表、冻结列、灰色前置条件区及报雷展示。

本版本为产值看板顶部五项汇总指标增加下钻能力，点击后以标题为“产值报表”的默认全屏弹框嵌入实际产值上报界面，复用查询、统计筛选、标准表格、分页、列设置和导出交互。

本版本继续下移图表横坐标公司名称和月份，将旋转标签偏移增加到 14px，彻底避开 0 轴。

本版本修复图表横坐标公司名称和月份与 0 轴重叠的问题，增加旋转文字的 6px 下移间距。

本版本保持图表卡片 398px 高度不变，仅调整内部绘图区、横坐标位置及 0 轴间距。

本版本将实际产值上报情况、产值月度统计图表区域高度放大 20%，并将横坐标标签与图表 0 轴间距调整为 6px。

本版本将各公司产值完成率百分比与进度条间距调整为 6px。

本版本优化各公司产值完成情况表格：金额 14px、完成率 12px，进度条宽度缩减至原可用区域的 80%。

本版本将城市环境、上海能建的施工类产值调整为左侧总承包/管线上下排列占 2/3，右侧普通业态跨两行占 1/3。

本版本优化产值看板“全部”状态左右区域比例，放宽施工类产值、缩窄六大业态产值，并统一两侧高度为 492px。

本版本将实际产值上报情况和产值月度统计的折线节点统一为 6px 圆点，并补充两张图的柱形和折线图例说明。

本版本将施工类产值内部卡片指标数值统一调整为 16px，顶部汇总指标数值保持 18px。

本版本统一产值看板施工类产值区域字体：顶部汇总名称 14px、指标数值 18px、单位 12px，并覆盖全部及各子公司状态。

本版本按公司配置施工类产值业态宽度权重，覆盖上海路桥、城市环境、上海能建、城建设计、城市运营、数字集团和地空公司。

本版本优化市政集团施工类产值布局：总承包占 1/2，设计、产品销售各占 1/4。

本版本将产值看板“全部”状态的施工类产值区域高度同步为 492px，与各子公司状态完全一致。

本版本固定产值看板各子公司的施工类产值区域高度，与上海隧道保持一致，切换子公司时仅调整内部业态布局和数据。

本版本试点优化上海隧道产值看板施工类产值布局：总承包、管线占约 2/3，设计、产品销售占约 1/3。

本版本修复实际产值上报五个业态列宽在组合主表中被覆盖的问题，并将上报时间说明改为标题旁常驻展示。

本版本统一实际产值上报附件空状态为“未上传”，并在产值上报明细标题旁增加上报时间说明。

本版本优化实际产值上报的业态列和月度项目明细列宽，并支持上报时删除已上传的项目相关附件。

本版本将未绑定提示图标替换为红色 SVG，并与“未绑定”文字统一颜色和对齐方式。

本版本将生产项目列表中未关联核算项目、未关联施工项目的提示统一为“提示图标 + 未绑定”。

本版本修复订单项目选择器居中对齐被通用表格样式覆盖的问题，增加专用居中规则，确保指定字段实际居中展示。

本版本统一订单项目选择器指定字段居中展示，并将订单项目名称改为普通文本展示。

本版本将订单项目选择器表格调整为通用列表卡片结构，增加“订单项目列表”标题、刷新和列设置入口，并统一分页样式。

本版本统一订单项目选择器字段命名和对齐规范，将项目类型、所属区域改为标签展示。

本版本补充换绑联动：绑定订单项目后，订单项目选择器中的“关联生产项目”数量自动加 1；从原订单项目换绑到新订单项目时，原订单项目数量同步减 1。

本版本为生产项目列表所有记录增加“换绑”操作，新增标准订单项目选择器，支持条件查询与分页；确认选择后即时更新关联订单项目编号并反馈绑定成功。

本版本实现施工日志筛选结果批量导出：单天单文件直接下载，单天多文件生成日期 ZIP，多天生成总 ZIP 并按日期建立文件夹；在线记录自动生成 Word，文件上报保留原文件名。

本版本进一步统一施工日志 Word 的四等分布局：基础信息按项目名称整行、日期等四项、工区两份及记录人/上报时间各一份排列；风险信息按四行四等分规则重新组织。

本版本优化施工日志 Word 导出版式：删除副标题和底部说明，扩大项目名称、施工工区和记录人值域，加宽人员信息标签，并补齐风险日期、描述、挂牌领导、持续时间及独立整行的风险进展情况。

本版本支持单个在线上报施工日志导出 Word，自动将页面中的基础信息、人员信息、今日与明日工作、里程碑、风险及停工情况写入标准模板；企业端与项目端复用同一导出规则。

本版本重构国际版项目基本信息，将“项目属性”分组改为“经济基本信息”，按指定顺序整合 13 个项目、合同、利润、成本与资金字段；相关字段从原筹划、过程、利润汇率和结算分组移入，避免重复展示。

本版本修复国际版已签合同动态三组统计的数据重叠：字段和值按实际内容计算宽度，统计项之间固定保留 14px 间距，取消固定等分宽度；超出可视区域时继续使用横向滚动查看。

本版本完善国内版、国际版已签合同动态：在建合同结算价显示 `--` 且不参与合计，结算合同即使为 0 仍正常展示；国内版修正结算价合计列对齐；国际版新增实际签署合同额、产值计量额、结算价三组横向统计；两版合同类型统一标签展示。

本版本改造国际版已签合同动态：删除合同金额、信用代码、是否主体、分包事项和特征值列；信息获取时间后增加实际签署合同额和产值计量额；实际签署合同额、产值计量额和结算价均提供底部合计，并使用国际版独立 9 列宽度规则。

本版本调整项目经济基本信息国际版过程动态：删除“分包计量动态”，在原位置完整复用国内版“已签合同动态”的合同价统计、结算价统计、12 列合同明细、状态标签及合同金额/结算价合计，供后续国际版改造。

本版本优化项目经济基本信息：国际版“国际项目类型”统一改为“国际项目属性”并同步数据字典，删除项目状态与 COST 清单项合同实际签署总额；国内、国际主材料过程动态移除合同总用量行，国际版项目经济筹划补齐与国内版一致的六项主材料合同总用量。

本版本修复项目经济总览趋势悬浮卡片：解除阈值标注样式对统计指标文字的误覆盖，移除橙色指标名称块；悬浮月份热区提升层级，数据卡片完整遮盖其他月份圆点，避免干扰阅读。

本版本优化项目经济总览实时趋势：月份悬浮信息改为带轻阴影的白色数据卡片，按“诊断期数（yyyy年mm月）/ 统计指标 / 统计值”结构展示；实际值与阈值图例替换为指定 SVG，并内置到项目资源目录。

本版本优化生产纳统看板企业明细：行业、规上/规下、是否纳统、在地按指定默认宽度居中展示，所属公司统一居中；建筑业在每家单位小计之外新增固定底部总合计，并按当前公司筛选范围动态汇总。

本版本继续优化项目经济总览实时趋势卡片：标题调整为 12px，统计值调整为 16px 加粗且单位保持 12px regular，标题与统计值间距统一为 16px，趋势图高度提升至 100px。

本版本优化项目经济总览实时趋势分析：趋势图区域增高约 20%，标题和统计值收紧至 14px；五期模拟数据改为带升降波动的曲线；阈值线及标注统一为 60% 透明浅红色，趋势线统一为主色蓝；月度圆点保持正圆，悬浮数据卡片放大并支持突破单卡片边界展示。

本版本将项目经济总览“月度检验报告”及企业经济诊断“月度检验单、分析报告”的下载图标统一替换为指定 SVG，并将资源内置到项目目录，确保本地与部署环境均可正常加载。

本版本优化项目管理 > 经济 > 经济总览：国内版、国际版标题对齐企业大屏标题规范；实时项目数据标题改为 regular；项目卡片风险状态仅保留色块，并按对应预警色从 25% 到 0% 纵向渐变，无预警时使用主色渐变；四项提醒指标标题统一分行展示。

本版本新增“国际项目类型”数据字典，编码为 `INTERNATIONAL_PROJECT_TYPE`，依次初始化非港澳JV项目、港澳JV项目、非JV项目、JV项目、投资类（含类投资）、非投资类，并支持旧浏览器缓存自动补全。

本版本将项目经济总览预警明细等级替换为用户提供的一颗雷、二颗雷 SVG，模拟数据仅使用这两种等级；国际版一级风险状态和预警明细名称统一读取国际版经济预警指标数据字典，国内版保持原有命名。

本版本新增“经济预警指标（国际版）”两级数据字典：按 `GJ-XX` / `GJ-XX-XX` 识别一级、二级节点，初始化 4 个一级指标及 9 个二级指标，并兼容已有浏览器字典缓存自动补全。

本版本统一项目管理 > 经济 > 经济总览的可读性基线：项目卡片字段和值、预警明细表头及内容均不小于 14px，实时趋势分析卡片标题不小于 12px。

本版本修复项目管理右上角项目切换器只读取前 38 个项目的问题，切换列表现在完整引用施工项目一览全部项目，包含 CSV 初始化的城建国际项目；下拉列表支持滚动，可直接选择项目验证经济总览国际版。

本版本在企业经济诊断标题后新增“国内版、国际版”切换，国际版当前一比一复用国内版内容；同时加宽项目名称、项目类型、所属区域和诊断月份控件，并在窄屏下采用两行头部布局避免文字与图标重叠。

本版本将企业管理 > 经济 > 大屏看板拆分为“经济总览、经济诊断”两个三级菜单；经济诊断直接展示原诊断看板并移除页内 Tab，经济总览暂时提供独立占位页。

本版本优化项目经济总览趋势图的月份查看交互：捕捉对应月份时显示竖向虚线，并通过浮层卡片展示月份、指标名称及当月数值，首尾月份自动避让边界。

本版本优化项目管理 > 经济 > 经济总览：诊断期数复用通用年月选择组件；实时项目数据统一为生产总览指标样式；实时趋势分析改为近五期可悬浮查看期次和值的平滑曲线；一级指标风险预警按文字与色块自适应宽度展示。

本版本优化企业管理 > 经济 > 大屏看板 > 经济诊断：移除页面额外蓝灰色背景并沿用原内容间距；诊断项目清单改为一屏自适应列宽，缩小项目名称列，超长项目名称单行省略显示。

本版本精简企业管理 > 经济菜单，删除“成本总览、结算管理、经营分析、资金计划”，保留“大屏看板、合同管理、经济开项”，并同步调整菜单路由序号。

本版本优化企业管理顶部一级菜单跳转：点击“经济”后自动激活侧边“大屏看板”，并直接选中“经济诊断”Tab，无需再次点击大屏看板；同时修复反复切换业务条线后路由目录重复拼接的问题。

本版本继续优化企业管理 > 经济 > 大屏看板 > 经济诊断：六项头部指标始终保持单行；项目类型统一读取 `PROJECT_TYPE` 数据字典并使用字典标签样式；项目清单同步优化标题、组织列命名及重点列对齐；潜亏预警说明文字缩小为 14px，避免标题溢出。

本版本将企业管理“经济”一级菜单的默认落点设置为“大屏看板 > 经济诊断”，进入经济条线后直接展示经济诊断内容；经济总览仍可通过 Tab 正常切换。

本版本将经济大屏全面对齐生产大屏规范：复用同款 Logo、标题字体字号、头部高度、Tab 样式及指标组件；页面左右留白调整为标准 10px，首行六项指标统一采用生产大屏的标题、数字、单位和图标结构。

本版本在保留经济开项审批功能基础上，新增企业管理 > 经济 > 大屏看板，标题为“数智施工项目经济管理平台”，提供“经济总览 / 经济诊断”双 Tab；经济诊断初步还原组织切换、项目/类型/省市/区域/月度筛选、项目与合同指标、四色预警指标、四类经济预警卡及标准明细项目清单。

本版本在企业管理 > 经济新增“经济开项 > 开项审批”：每月按子公司自动生成上月新立项项目清单，支持补录上月之前未完成开项的历史项目；子公司逐项目标注是否上线及不上线理由后提交，产运部可调整并确认结束。页面复用标准查询卡、列表、列设置、弹框及流程卡片，并将已完成审批结果沉淀到 `window.economyProjectInitiationApprovedData` 供后续功能复用。

本版本将待办触达批次的发送状态字典、查询条件和统计筛选统一为消息发送批次同一套：待发送、已发送、部分发送、失败、已撤回；撤回后的聚合状态同步正确回显。

本版本明确消息与待办批次边界：消息发送批次的查询、类型统计和列表数据均排除待办任务，待办任务统一在待办触达批次中管理。

本版本为待办触达批次新增“待办内容”和“触达用户姓名”查询条件；姓名查询遍历批次全部应发接收人，只要任一姓名包含输入内容即可匹配该批次。

本版本完成待办触达批次标准列表：由待办触达明细按批次号实时聚合，展示发送、触达、阅读、点击、办理等人数与比率，以及触发和跳转配置；支持标准查询、统计、列设置、分页、查看、重新触达与撤回。

本版本将菜单和列表页标题统一更名为“发送批次明细”，标题后新增“消息发送批次 / 待办触达批次”双 Tab；消息发送批次保留原页面全部功能。

本版本将发送批次记录详情中的消息标题与内容、待办标题与内容调整为上下分行展示，标题在上、内容在下。

本版本修复发送批次记录详情中触达统计与明细区域受信息栅格限制的问题，区域横向铺满弹框内容区并延伸至右侧边缘，表格内部保持独立横向滚动。

本版本将“发送记录详情”更名为“发送批次记录详情”，并以可筛选的送达、阅读、点击统计和标准触达明细表格替换原接收人明细预览。

本版本将消息&待办管理相关列表中的“业务分类”默认列宽统一调整为 160px。

本版本统一调整消息管理导航命名：消息管理改为“消息&待办管理”，子菜单改为“模板管理 / 发送批次记录 / 用户触达明细”；用户触达明细的消息 Tab 表格标题同步调整为“消息发送明细”。

本版本继续优化消息记录：消息发送明细与待办触达明细统一复用同一套二级业务分类树及多选筛选交互，待办模拟数据同步调整为二级分类；两张明细表的发送批次号改为蓝色可点击链接，可下钻查看对应发送批次详情及接收人明细。

本版本将消息记录与产值预测分析报表标题后的 Tab 及对应标题行高度由 42px 统一调整为 36px，继续保持 16px 字号和既有按钮宽度。

本版本统一优化消息记录与产值预测分析报表标题后的 Tab：按钮高度调整为 42px，文字固定为 16px；产值预测 Tab 宽度由 154px 缩小至 150px，消息记录 Tab 缩小至 132px，并保持长标题单行展示。

本版本明确消息记录的类型边界：消息发送明细仅展示“消息通知、预警通知、通知公告”三类消息，查询条件同步移除待办任务；待办任务仅在“待办触达明细”中展示和管理。

本版本优化企业管理 > 基础 > 消息管理 > 消息记录：标题后新增“消息发送明细 / 待办触达明细”双 Tab，首个 Tab 保持原消息触达界面；新增待办触达标准列表，覆盖业务分类、待办内容、接收范围、送达、阅读、点击、办理、批次与失败原因等字段，并支持独立筛选、状态统计、详情查看和重新推送。

本版本继续优化企业管理 > 基础 > 消息管理 > 消息模板：接收人员类型选择“指定人员”时，使用全局二级弹框展示左侧组织树和右侧人员列表；组织树读取组织管理实时数据，人员读取组织人员主数据，支持组织联动、姓名/账号/手机号搜索、分页、当前页全选、跨组织多选及确认/取消。选择结果内部保存人员唯一标识，主表单以首项标签和 `+N` 单行回显，发送记录展示人员姓名；已禁用人员不可回填或发送，未选择人员时阻止保存；待办超期通知同步复用相同选择器。

本版本继续优化企业管理 > 基础 > 消息管理 > 消息模板：接收人员类型选择“动态参数”时，右侧改为与“选择接口”相同样式的标准单选下拉框；企业端、项目端加载各自动态参数，引用模板时自动回填，发送时读取当前单选结果。待办超期通知的动态参数选择同步采用相同控件。

本版本继续优化企业管理 > 基础 > 消息管理 > 消息模板的组织选择：父节点勾选时自动全选其全部子孙组织，取消父节点时同步取消后代；只选择部分子节点时，所属子公司和隧道股份节点显示半选状态。输入框按最小有效范围回显，整家子公司全选时仅回显子公司，不逐项铺开其分公司；同名组织通过完整路径区分，裸名称仅在唯一匹配时自动回填。

本版本继续优化企业管理 > 基础 > 消息管理 > 消息模板：接收人员类型选择“组织”时，按组织管理中的实时数据展示“隧道股份 / 子公司 / 分公司”三级树；各层级节点支持独立混合多选、展开收起、搜索、清空和标签删除；多选结果以首项标签和 `+N` 单行折叠回显。内部使用组织编码区分同名分公司，消息发送记录使用完整组织路径展示。

本版本优化企业管理 > 基础 > 消息管理 > 消息模板：接收人员类型选择“岗位”时，右侧改为标准多选下拉组件，支持岗位搜索、全选、清空、逐项勾选和标签删除；选中多个岗位时以首项标签和 `+N` 折叠展示，并按企业端、项目端接收层级加载对应岗位数据。

本版本补充安全评价模型发布状态联动：模型编辑保存后发布状态变为“待发布”并显示发布按钮；点击发布生成新版本后，发布状态恢复为“已发布”；当前模型状态筛选同步增加“待发布”。

本版本继续优化企业管理 > 安全 > 安全评价 > 安全评价模型：操作列默认 200px、模型编码默认 160px；新增模型提示当前 v1.0 暂不支持；版本记录弹框移除模型名称提示行；模型编辑保存后才显示发布按钮，发布时生成新版本；当前模型列表暂时移除启用、停用操作。

本版本调整企业管理 > 首页 > 施工日志项目下钻弹框：施工日志列表导出按钮移动到标题行最右侧，导出逻辑和次要按钮样式保持不变。

本版本继续完善施工日志：项目管理施工日志在在线上报、文件上报后新增次要导出按钮，按当前筛选结果导出施工日志文件；文件上报的施工日志文件支持点击预览提示；企业管理项目下钻的“施工日志列表”标题后新增同规则导出按钮。

本版本继续优化企业管理 > 首页 > 施工日志：项目名称改为普通黑色文本并统一从查看按钮下钻；移除施工日志上报明细导出按钮；项目下钻日志卡片悬浮时显示靠右导出操作；施工日志详情改为叠加在项目弹框之上的二级弹框，关闭详情后保留项目弹框。

本版本继续优化企业管理 > 安全 > 安全评价 > 安全评价模型：列表删除批量发布按钮和发布时间起止查询条件；适用对象类型将“敢为”修正为“岗位”；模型状态删除“版本中”。历史功能中的评价模型保持不变。

本版本优化企业管理 > 安全 > 安全评价 > 安全评价模型：模型结构新增“总分”根节点，形成总分、指标分组、指标三级结构；总分节点支持配置五个分组的百分比权重并动态计算分组及指标分值；指标列表标题、对齐和序号列宽同步优化。历史功能中的评价模型保持不变。

本版本优化企业管理 > 安全 > 安全评价 > 评价结果管理：评价对象数拆分为评价项目数、评价分公司数、评价子公司数；综合得分调整为项目综合得分；查看详情调整为查看；删除批量确认、批量归档，以及评价模型、风险等级、对象类型查询条件。

本版本优化纳统看板底部区域：右侧明细接入标准分页并移除标题区范围/行数信息；合计行固定在表格滚动区底部并位于横向滚动条上方；左侧公司名称列缩窄并为最右侧进度条保留稳定空间。

本版本继续优化纳统看板：顶部五项指标直接复用产值看板指标组件；六类业态卡片高度调整为 280px 并优化内部间距；纳统率调整为名称、进度条、百分比单行布局；右上角年月选择器复用产值看板组件及未来月份限制规则。

本版本按名称替换企业管理 > 生产 > 大屏看板 > 纳统看板顶部五个统计指标图标，使用指定 SVG 资源，指标数据与交互保持不变。

本版本仅优化企业管理 > 生产 > 大屏看板 > 纳统看板 UI：统一浅灰页面背景、白色紧凑卡片和蓝色主色，优化五项统计指标、六类业态卡片、公司维度列表和纳统企业明细表格布局；原有业态切换、公司下钻、列设置和分页交互保持不变。

本版本继续优化企业管理 > 安全 > 安全评价 > 安全评价任务：选择评价项目标题右侧移除项目标签；项目经理列再次缩小至当前宽度的 80%；任务列表在评价子公司数后增加评价结果数量，点击数量可下钻查看该任务每次执行生成的评价结果数据。

本版本继续优化企业管理 > 安全 > 安全评价 > 安全评价任务：新增和编辑选择手动执行时隐藏执行时间字段；查看任务时按执行方式动态展示执行时间，不再展示任务状态，并补充任务描述；查看页“选择评价项目”标题后展示项目、分公司、子公司实时范围统计和自动去重说明。

本版本继续优化企业管理 > 安全 > 安全评价 > 评价结果管理：评价结果列表删除评价对象、评价模型、模型版本三列，其他结果字段、筛选和确认规则保持不变。

本版本重新划分安全评价任务与评价结果职责：评价任务每一期只允许一条，状态仅保留未执行、执行中、已执行；任务支持常态编辑、执行和重新执行，每次执行完成新增一条待确认评价结果。确认发布移至评价结果管理，同一期只允许一条结果已确认，确认新结果时自动取消旧结果确认。

本版本继续优化企业管理 > 安全 > 安全评价 > 安全评价任务：由于诊断为秒级执行，列表移除任务进度列；工具栏移除批量执行按钮。历史“评价任务管理”继续保留原列和原按钮。

本版本继续优化企业管理 > 安全 > 安全评价 > 安全评价任务：同一期数允许创建并执行多个诊断任务，但只允许一个任务处于“已确认”状态；确认同一期新任务时，原已确认任务自动退回“已执行”，当前任务成为该期唯一确认发布结果。

本版本继续优化企业管理 > 安全 > 安全评价 > 安全评价任务：将已选项目、涉及分公司、涉及子公司及自动去重说明移动到“选择评价项目”标题后，并增加信息标识；项目选择表的子公司列缩小至原宽度 80%、项目经理列缩小至 80%、分公司列缩小至 90%。

本版本继续优化企业管理 > 安全 > 安全评价 > 安全评价任务：列表复选框后、任务编号前增加序号列；操作列默认 180px、任务编号 140px、任务名称 180px、任务开始及结束时间各 150px、创建人 80px、创建时间 150px。

本版本继续优化企业管理 > 安全 > 安全评价 > 安全评价任务：创建和查看时按“项目安全评价模型 ➡️ 分公司安全评价模型 ➡️ 子公司安全评价模型”展示递进关系；项目选择表增加序号、项目经理、安全纳管状态和项目编号，并调整字段顺序；新增项目名称、子公司、分公司、安全纳管状态、项目经理、项目状态、项目编号七项筛选条件。

本版本调整企业管理 > 安全 > 安全评价 > 安全评价任务：一个任务固定同时绑定子公司、分公司、项目三个安全评价模型；新增任务时仅选择参与评价的项目，并根据项目归属自动去重计算评价分公司数、评价子公司数，确保三个层级模型在同一评价范围内一次执行。任务列表补充任务期数和未执行、执行中、已执行、已确认四态流转。历史功能中的“评价任务管理”保持原逻辑。

本版本继续优化企业管理 > 安全 > 安全评价 > 安全评价模型：权重输入框参考项目产值上报的单位输入框，采用整体边框、内部无边框输入和右侧 `%` 单位；当当前分组权重合计超过 100% 时，该分组全部权重输入框统一显示红色外框，恢复至 100% 或以下后自动恢复正常。

本版本继续优化企业管理 > 安全 > 安全评价 > 安全评价模型：补全与查看详情一致的编辑弹框，权重使用带 `%` 单位的输入框；每个分组的指标权重合计必须为 100% 才能保存；指标分值按“分组分值 × 权重”实时计算并同步更新模型树。

本版本继续优化企业管理 > 安全 > 安全评价 > 安全评价模型：详情弹框的指标列表在最前增加“序号”，在“分值”前增加取值范围 0~100 的“权重”，并移除“是否扣分”。历史功能中的“评价模型”详情保持原列结构。

本版本调整企业管理 > 安全 > 安全评价 > 安全评价模型：建立与历史“评价模型”隔离的独立模式，仅保留子公司、分公司、项目三条安全评价模型示例数据；适用对象类型字典调整为子公司、分公司、项目、敢为、供应链；移除配置指标、复制、删除操作；版本记录支持下钻查看操作类型、操作人、生效时间和版本摘要。历史功能中的“评价模型”不受影响。

本版本继续优化企业管理 > 安全 > 安全评价 > 月度评价填报：恢复序号列的展开、收起 SVG 图标并增加资源缓存版本；将“险情信息填报”分组整体移动至“实名四要素一致性”之后，计算和交互保持不变。

本版本继续优化企业管理 > 安全 > 安全评价 > 月度评价填报：查询条件与评价数据拆分为两个独立标准区域；编辑、保存、取消后保持表格原有横向及纵向滚动位置；操作列使用贯穿整个冻结区域的连续阴影，不再逐单元格显示阴影。

本版本优化企业管理 > 安全 > 安全评价 > 月度评价填报：新增“险情信息填报”分组及“是否知情不报”“得分”两列。项目行在编辑状态下可选择是/否并实时计算得分；分公司、子公司按所属项目自动汇总，分别按每个知情不报项目扣 10 分、5 分。

本版本在企业管理 > 安全下新增“历史功能”菜单分组，并将“对象管理（禁）”“评价模型”“评价任务管理”从“安全评价”迁移至该分组；功能页面、数据和交互保持不变。

本版本在企业管理 > 安全 > 安全评价下新增“安全评价模型”和“安全评价任务”两个菜单，分别完整复用现有“评价模型”和“评价任务管理”的页面内容、数据及交互，原菜单继续保留。

本版本优化企业管理 > 生产 > 产值管理 > 实际产值上报：列表默认冻结前五列至“填报情况”；除“合计”外的业态列默认宽度设为 100px，其中“投资【租赁及保理】”设为 180px。

本版本恢复平台菜单的稳定导航，修复部分预览环境下菜单无法点击的问题。菜单 URL 使用非阻断方式同步：即使当前环境限制 History API，仍会继续执行原菜单渲染；切换过程不再触发整页刷新，从而消除闪屏。

本版本修复平台全局菜单切换时的快速闪屏：菜单点击改为同页更新路由并直接渲染目标模块，不再触发整页重新加载；浏览器前进、后退同步恢复对应菜单和页面状态。

本版本优化企业管理 > 安全 > 安全评价 > 月度评价填报：序号列默认宽度调整为 80px；已结束的填报任务仅允许查看，不再展示“提交”操作。

本版本优化企业管理 > 安全 > 安全评价 > 月度评价填报：修复跨两行的操作列表头在横向滚动时下半区域被覆盖的问题，并将操作列默认宽度调整为 100px。

本版本升级全局标准列表组件：所有分页默认调整为 50 条/页；列设置新增“冻结至第 X 列”，支持用户配置左侧冻结列数，操作列继续固定在右侧，仅中间内容横向滚动，并支持页面级默认冻结列数。

本版本优化安全评价月度评价填报：树形图标与序号居中且间距调整为 2px，项目行默认只读并新增操作列，支持按行编辑、保存和取消，保存后提示成功，取消不写入修改。

本版本优化企业管理首页施工日志：操作列接入标准冻结规则，新增在线上报、文件上报方式统计筛选，查询区增加项目状态和项目编号高级条件，项目经理统一展示姓名、加密手机号及查看图标。

本版本优化安全评价月度评价填报：子公司列默认宽度调整为 120px，项目名称表头居中，并使用指定 SVG 替换公司和分公司的展开、收起图标。

本版本优化安全管理劳务工花名册：操作列默认宽度调整为 160px，详情、编辑、删除操作之间取消竖线分隔，并继续保持操作列冻结效果。

本版本修复评价任务管理等自定义列表中仅操作表头冻结、表体操作内容未冻结的问题；所有基于全局列配置的自定义列表均同步应用操作列冻结，并增强冻结列左侧外投影。

本版本优化全局标准列表组件：操作列统一固定在表格最右侧，横向滚动时表头与内容保持冻结，仅其他业务列移动，并增加固定列分隔阴影和悬停状态适配。

本版本完善企业管理生产实际产值上报的草稿流程：上报和编辑界面统一提供取消、保存、发起上报；保存后保持未上报并可恢复草稿继续编辑，发起后进入上报审批中且只能查看。

本版本优化企业管理产运接口同步异常记录：生产项目名称、编号调整为通用的数据名称、数据编号，删除子公司项目编号，新增数据类型、接口名称字段，并增加数据类型查询条件。

本版本补全企业管理生产实际产值上报的组织范围控制：分公司层级仅展示本分公司的上报明细并自动生成当月明细行；子公司层级仅展示其下属分公司数据；查询与重置操作不会突破当前组织的数据权限范围。

本版本优化企业管理生产实际产值上报的相关附件布局：上报态不展示空状态，上传按钮位于提示语下方；查看态的附件或“暂无附件”位于提示语下方，多个附件逐行排列。

本版本基于同事协作调整后的产值模块继续开发，将企业管理生产产值看板中的“产值进度”和相关“完成率”指标统一命名为“产值完成率”。

本版本同步施工日志详情字段：文件上报日志在项目端详情和企业端下钻详情中，基础信息均仅显示工区和日期。

本版本优化施工日志上报字段：文件上报基础信息仅保留工区和日期；在线上报温度输入框增加 `℃` 单位。

本版本建立项目管理统一项目上下文。右上角项目切换读取项目主数据，切换后同步刷新项目总览、项目详情、工程总体筹划和施工日志；企业管理继续以项目明细为基础进行汇总。

本版本统一施工日志示例数据：企业端与项目端共用 38 个项目在 2026 年 6 月 18 日至 7 月 13 日的上报状态和日志记录；更早日期及 7 月 13 日之后显示为未开始。

## Local Master Data

This version adds local maintenance for organizations, users, projects, labor workers, and data dictionaries.
Data is saved in browser localStorage and persists after refresh. Use `Base > Organization Management` to export, import, or restore the complete master-data package.

See `docs/master-data-guide.md` and `data/master-data-schema.json` for the data model and backend handoff reference.
