# 数智施工 Demo 开发约定

## 开发目录

- 用户指定的开发目录为 `C:\Users\loulidong\Desktop\新建文件夹\Digital-Intelligent-Construction`，直接在本目录进行开发。
- 不要将 Documents 下的同名项目或历史版本目录作为当前开发基线。
- 优先复用现有组件、交互和样式，页面文字使用中文。
- 修改前检查工作区，保留用户已有的未提交改动；手工编辑使用 apply_patch。

## 版本备案与回退

- 每次需求开始修改前，将当前工作区的项目文件备份到项目同级的 `Digital-Intelligent-Construction-版本备案` 目录，使用版本号、时间和需求简称命名，标记为“开发前”。备份必须包含已有的未提交业务文件改动。
- 备份排除 `.git`、`node_modules`、`.wrangler`、`tmp` 和临时缓存；不要覆盖已有备份。
- 每次需求执行完成后递增项目版本，同步更新实际使用的版本常量、入口资源缓存参数、README.md 和 CHANGELOG.txt 中的版本信息。
- 完成必要的语法检查和浏览器交互验证后，在上述备案目录保存“完成版”ZIP 快照，确保其包含运行 Demo 所需的源码、数据和静态资源。
- 在备案目录的版本记录中写明版本号、时间、需求摘要、修改文件、验证结果、开发前及完成版备份路径、当时的 Git HEAD 和工作区状态。未通过或未执行的验证须如实记录。
- 每次交付向用户说明版本号、主要变更、验证结果和备份位置。
- 回退时先备份当前状态，再恢复用户指定的版本；不得直接清理或覆盖未备份的工作。
- 用户未要求发布时，不自动推送远端或部署。
