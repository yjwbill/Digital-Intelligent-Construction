# 数智施工演示环境

这是一个用于产品演示的静态前端 Demo，已整理为适合 GitHub Pages 发布的目录结构。

## 在线发布

将本目录中的所有文件上传到 GitHub 仓库根目录，然后开启 GitHub Pages：

1. 进入仓库 `Settings`。
2. 打开 `Pages`。
3. Source 选择 `Deploy from a branch`。
4. Branch 选择 `main`，Folder 选择 `/root`。
5. 保存后等待 GitHub 生成访问地址。

访问地址通常为：

```text
https://你的用户名.github.io/仓库名/
```

## 目录结构

```text
.
├── index.html              # 页面入口
├── app.js                  # 页面逻辑与模拟数据
├── styles.css              # 全局样式
├── organization-data.js    # 组织基础数据
├── src/                    # 图片、图标、组件资源
├── data/                   # 演示数据与数据结构占位
├── docs/                   # 文档资料
├── CHANGELOG.txt           # 历史版本说明
└── .nojekyll               # GitHub Pages 静态资源开关
```

## 本地预览

可以直接使用任意静态服务器预览，例如：

```bash
npx serve .
```

或使用 VS Code 的 Live Server 插件打开 `index.html`。

## 注意事项

- GitHub Pages 只能托管静态文件，不能运行后端服务。
- 当前在线版本适合页面演示、交互预览和同事评审。
- 数据字典、组织、人员等在线维护后的服务端持久化，需要后续接入后端接口或使用本地服务版本。

## 当前版本

`EM-20260701-V2.2.347-PRODUCTION-STATISTICS-TABLE-LAYOUT`

本版本优化纳统看板底部区域：右侧明细接入标准分页并移除标题区范围/行数信息；合计行固定在表格滚动区底部并位于横向滚动条上方；左侧公司名称列缩窄并为最右侧进度条保留稳定空间。

本版本继续优化纳统看板：顶部五项指标直接复用产值看板指标组件；六类业态卡片高度调整为 280px 并优化内部间距；纳统率调整为名称、进度条、百分比单行布局；右上角年月选择器复用产值看板组件及未来月份限制规则。

本版本按名称替换企业管理 > 生产 > 大屏看板 > 纳统看板顶部五个统计指标图标，使用指定 SVG 资源，指标数据与交互保持不变。

本版本仅优化企业管理 > 生产 > 大屏看板 > 纳统看板 UI：统一浅灰页面背景、白色紧凑卡片和蓝色主色，优化五项统计指标、六类业态卡片、公司维度列表和纳统企业明细表格布局；原有业态切换、公司下钻、列设置和分页交互保持不变。

本版本继续优化企业管理 > 安全 > 安全评价 > 安全评价任务：选择评价项目标题右侧移除项目标签；项目经理列再次缩小至当前宽度的 80%；任务列表在评价子公司数后增加评价结果数量，点击数量可下钻查看该任务每次执行生成的评价结果数据。

本版本继续优化企业管理 > 安全 > 安全评价 > 安全评价任务：新增和编辑选择手动执行时隐藏执行时间字段；查看任务时按执行方式动态展示执行时间，不再展示任务状态，并补充任务描述；查看页“选择评价项目”标题后展示项目、分公司、子公司实时范围统计和自动去重说明。

本版本继续优化企业管理 > 安全 > 安全评价 > 评价结果管理：评价结果列表删除评价对象、评价模型、模型版本三列，其他结果字段、筛选和确认规则保持不变。

本版本重新划分安全评价任务与评价结果职责：评价任务每一期只允许一条，状态仅保留未执行、执行中、已执行；任务支持常态编辑、执行和重新执行，每次执行完成新增一条待确认评价结果。确认发布移至评价结果管理，同一期只允许一条结果已确认，确认新结果时自动取消旧结果确认。

本版本继续优化企业管理 > 安全 > 安全评价 > 安全评价任务：由于诊断为秒级执行，列表移除任务进度列；工具栏移除批量执行按钮。历史“评价任务管理”继续保留原列和原按钮。

本版本继续优化企业管理 > 安全 > 安全评价 > 安全评价任务：同一期数允许创建并执行多个诊断任务，但只允许一个任务处于“已确认”状态；确认同一期新任务时，原已确认任务自动退回“已执行”，当前任务成为该期唯一确认发布结果。

本版本继续优化企业管理 > 安全 > 安全评价 > 安全评价任务：将已选项目、涉及分公司、涉及子公司及自动去重说明移动到“选择评价项目”标题后，并增加信息标识；项目选择表的子公司列缩小至原宽度 80%、项目经理列缩小至 80%、分公司列缩小至 90%。

本版本继续优化企业管理 > 安全 > 安全评价 > 安全评价任务：列表复选框后、任务编号前增加序号列；操作列默认 180px、任务编号 140px、任务名称 180px、任务开始及结束时间各 150px、创建人 80px、创建时间 150px。

本版本继续优化企业管理 > 安全 > 安全评价 > 安全评价任务：创建和查看时按“项目安全评价模型 ➡️ 分公司安全评价模型 ➡️ 子公司安全评价模型”展示递进关系；项目选择表增加序号、项目经理、安全纳管状态和项目编号，并调整字段顺序；新增项目名称、子公司、分公司、安全纳管状态、项目经理、项目状态、项目编号七项筛选条件。

本版本调整企业管理 > 安全 > 安全评价 > 安全评价任务：一个任务固定同时绑定子公司、分公司、项目三个安全评价模型；新增任务时仅选择参与评价的项目，并根据项目归属自动去重计算评价分公司数、评价子公司数，确保三个层级模型在同一评价范围内一次执行。任务列表补充任务期数和未执行、执行中、已执行、已确认四态流转。历史功能中的“评价任务管理”保持原逻辑。

本版本继续优化企业管理 > 安全 > 安全评价 > 安全评价模型：权重输入框参考项目产值上报的单位输入框，采用整体边框、内部无边框输入和右侧 `%` 单位；当当前分组权重合计超过 100% 时，该分组全部权重输入框统一显示红色外框，恢复至 100% 或以下后自动恢复正常。

本版本继续优化企业管理 > 安全 > 安全评价 > 安全评价模型：补全与查看详情一致的编辑弹框，权重使用带 `%` 单位的输入框；每个分组的指标权重合计必须为 100% 才能保存；指标分值按“分组分值 × 权重”实时计算并同步更新模型树。

本版本继续优化企业管理 > 安全 > 安全评价 > 安全评价模型：详情弹框的指标列表在最前增加“序号”，在“分值”前增加取值范围 0~100 的“权重”，并移除“是否扣分”。历史功能中的“评价模型”详情保持原列结构。

本版本调整企业管理 > 安全 > 安全评价 > 安全评价模型：建立与历史“评价模型”隔离的独立模式，仅保留子公司、分公司、项目三条安全评价模型示例数据；适用对象类型字典调整为子公司、分公司、项目、敢为、供应链；移除配置指标、复制、删除操作；版本记录支持下钻查看操作类型、操作人、生效时间和版本摘要。历史功能中的“评价模型”不受影响。

本版本继续优化企业管理 > 安全 > 安全评价 > 月度评价填报：恢复序号列的展开、收起 SVG 图标并增加资源缓存版本；将“险情信息填报”分组整体移动至“实名四要素一致性”之后，计算和交互保持不变。

本版本继续优化企业管理 > 安全 > 安全评价 > 月度评价填报：查询条件与评价数据拆分为两个独立标准区域；编辑、保存、取消后保持表格原有横向及纵向滚动位置；操作列使用贯穿整个冻结区域的连续阴影，不再逐单元格显示阴影。

本版本优化企业管理 > 安全 > 安全评价 > 月度评价填报：新增“险情信息填报”分组及“是否知情不报”“得分”两列。项目行在编辑状态下可选择是/否并实时计算得分；分公司、子公司按所属项目自动汇总，分别按每个知情不报项目扣 10 分、5 分。

本版本在企业管理 > 安全下新增“历史功能”菜单分组，并将“对象管理（禁）”“评价模型”“评价任务管理”从“安全评价”迁移至该分组；功能页面、数据和交互保持不变。

本版本在企业管理 > 安全 > 安全评价下新增“安全评价模型”和“安全评价任务”两个菜单，分别完整复用现有“评价模型”和“评价任务管理”的页面内容、数据及交互，原菜单继续保留。

本版本优化企业管理 > 生产 > 产值管理 > 实际产值上报：列表默认冻结前五列至“填报情况”；除“合计”外的业态列默认宽度设为 100px，其中“投资【租赁及保理】”设为 180px。

本版本恢复平台菜单的稳定导航，修复部分预览环境下菜单无法点击的问题。菜单 URL 使用非阻断方式同步：即使当前环境限制 History API，仍会继续执行原菜单渲染；切换过程不再触发整页刷新，从而消除闪屏。

本版本修复平台全局菜单切换时的快速闪屏：菜单点击改为同页更新路由并直接渲染目标模块，不再触发整页重新加载；浏览器前进、后退同步恢复对应菜单和页面状态。

本版本优化企业管理 > 安全 > 安全评价 > 月度评价填报：序号列默认宽度调整为 80px；已结束的填报任务仅允许查看，不再展示“提交”操作。

本版本优化企业管理 > 安全 > 安全评价 > 月度评价填报：修复跨两行的操作列表头在横向滚动时下半区域被覆盖的问题，并将操作列默认宽度调整为 100px。

本版本升级全局标准列表组件：所有分页默认调整为 50 条/页；列设置新增“冻结至第 X 列”，支持用户配置左侧冻结列数，操作列继续固定在右侧，仅中间内容横向滚动，并支持页面级默认冻结列数。

本版本优化安全评价月度评价填报：树形图标与序号居中且间距调整为 2px，项目行默认只读并新增操作列，支持按行编辑、保存和取消，保存后提示成功，取消不写入修改。

本版本优化企业管理首页施工日志：操作列接入标准冻结规则，新增在线上报、文件上报方式统计筛选，查询区增加项目状态和项目编号高级条件，项目经理统一展示姓名、加密手机号及查看图标。

本版本优化安全评价月度评价填报：子公司列默认宽度调整为 120px，项目名称表头居中，并使用指定 SVG 替换公司和分公司的展开、收起图标。

本版本优化安全管理劳务工花名册：操作列默认宽度调整为 160px，详情、编辑、删除操作之间取消竖线分隔，并继续保持操作列冻结效果。

本版本修复评价任务管理等自定义列表中仅操作表头冻结、表体操作内容未冻结的问题；所有基于全局列配置的自定义列表均同步应用操作列冻结，并增强冻结列左侧外投影。

本版本优化全局标准列表组件：操作列统一固定在表格最右侧，横向滚动时表头与内容保持冻结，仅其他业务列移动，并增加固定列分隔阴影和悬停状态适配。

本版本完善企业管理生产实际产值上报的草稿流程：上报和编辑界面统一提供取消、保存、发起上报；保存后保持未上报并可恢复草稿继续编辑，发起后进入上报审批中且只能查看。

本版本优化企业管理产运接口同步异常记录：生产项目名称、编号调整为通用的数据名称、数据编号，删除子公司项目编号，新增数据类型、接口名称字段，并增加数据类型查询条件。

本版本补全企业管理生产实际产值上报的组织范围控制：分公司层级仅展示本分公司的上报明细并自动生成当月明细行；子公司层级仅展示其下属分公司数据；查询与重置操作不会突破当前组织的数据权限范围。

本版本优化企业管理生产实际产值上报的相关附件布局：上报态不展示空状态，上传按钮位于提示语下方；查看态的附件或“暂无附件”位于提示语下方，多个附件逐行排列。

本版本基于同事协作调整后的产值模块继续开发，将企业管理生产产值看板中的“产值进度”和相关“完成率”指标统一命名为“产值完成率”。

本版本同步施工日志详情字段：文件上报日志在项目端详情和企业端下钻详情中，基础信息均仅显示工区和日期。

本版本优化施工日志上报字段：文件上报基础信息仅保留工区和日期；在线上报温度输入框增加 `℃` 单位。

本版本建立项目管理统一项目上下文。右上角项目切换读取项目主数据，切换后同步刷新项目总览、项目详情、工程总体筹划和施工日志；企业管理继续以项目明细为基础进行汇总。

本版本统一施工日志示例数据：企业端与项目端共用 38 个项目在 2026 年 6 月 18 日至 7 月 13 日的上报状态和日志记录；更早日期及 7 月 13 日之后显示为未开始。

## Local Master Data

This version adds local maintenance for organizations, users, projects, labor workers, and data dictionaries.
Data is saved in browser localStorage and persists after refresh. Use `Base > Organization Management` to export, import, or restore the complete master-data package.

See `docs/master-data-guide.md` and `data/master-data-schema.json` for the data model and backend handoff reference.
